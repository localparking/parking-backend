spring:
  profiles:
    include: secret
  application:
    name: temp
  jpa:
    hibernate:
      ddl-auto: create

    show_sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
logging:
  level:
    org.springframework.security: DEBUG